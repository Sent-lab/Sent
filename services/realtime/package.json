{
  "name": "@sent/realtime-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebSocket gateway: subscriptions, reconnect replay, backpressure.",
  "exports": {
    ".": "./src/server.ts",
    "./gateway": "./src/gateway.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types sim/gateway.ts",
    "start": "node --experimental-strip-types src/main.ts"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sent/realtime": "workspace:*",
    "@sent/config": "workspace:*",
    "viem": "^2.56.3",
    "@sent/database": "workspace:*"
  }
}
