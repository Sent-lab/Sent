{
  "name": "@sent/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reorg-safe HyperEVM event ingestion. Produces the projection per Masterplan 138.",
  "dependencies": {
    "viem": "^2.56.3",
    "@sent/database": "workspace:*",
    "@sent/contracts": "workspace:*",
    "@sent/worker": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types sim/reorg.ts"
  }
}
